import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#DED5D0',
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'row'
  },
  text:{
    width:100,
    height:100,
    backgroundColor: 'blue',
    justifyContent: 'center',
    alignItems: 'center',
  }


})
export default styles;